"""Core domain layer: models, decision logic, and the region registry.

Per SAD Section 1.3, `core` sits above `data`/`storage`/`utils` in the
dependency graph and is imported by every higher layer.
"""
