"""Business/decision logic: risk scoring and recommendations.

Implemented in a later phase per the project roadmap; this package boundary
is established now so `app.controllers` can import against a stable
interface without circular dependencies.
"""
